[
  {
    "task": "Write a Python function that parses an AWS S3 bucket name from an S3 URI in the format 's3://bucket-name/key/path' and returns just the bucket name."
  },
  {
    "task": "Create a JSON object that represents an AWS IAM policy allowing read-only access to all objects in an S3 bucket named 'my-data-bucket'."
  },
  {
    "task": "Write a regular expression that validates an AWS EC2 security group ID, which follows the format 'sg-' followed by exactly 17 hexadecimal characters (e.g., 'sg-0a1b2c3d4e5f6g7h8')."
  }
]